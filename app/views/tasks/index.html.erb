<% @tasks.each do |task| %>
  <h2><%= task.name %></h2>
  <p><%= task.content %></p>
<%= link_to "delete", task_path(task),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
<% end %>

